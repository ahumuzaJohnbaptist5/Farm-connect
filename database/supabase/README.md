# Supabase Setup

1. Create a Supabase project.
2. Copy the SQL from `/database/schema.sql` into the Supabase SQL editor and run it.
3. Configure backend environment variables in `/config/.env.backend`.
